# Copy this file to .env.local and fill in values.
# .env.local is gitignored and used by both Next.js and docker-compose.

# Pre-fills the Glean backend URL on the setup screen.
# Override if your Glean instance has a different URL.
NEXT_PUBLIC_GLEAN_BACKEND_URL=https://scio-prod-be.glean.com
